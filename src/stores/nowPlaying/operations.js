import actions from "./actions";

const {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
    updateTime,
    toggleMute,
    setVolume,
} = actions;

export default {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
    updateTime,
    toggleMute,
    setVolume,
};
