import actions from "./actions";

const {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
    updateTime,
} = actions;

export default {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
    updateTime,
};
