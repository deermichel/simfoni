import actions from "./actions";

const {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
} = actions;

export default {
    playQueue,
    togglePlayback,
    skipForward,
    skipBackward,
    seek,
};
